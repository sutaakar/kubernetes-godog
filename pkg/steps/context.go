package steps

// Context carries contextual information related to step execution and results
var Context struct {
	// ActiveNamespace stores latest created or used namespace
	ActiveNamespace string
}
